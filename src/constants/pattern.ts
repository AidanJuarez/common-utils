export const tokenPattern = /^\s*0x[0-9a-fA-F]{40}\s*$/;

export const contractMethodIdPattern = /^0x[0-9A-Fa-f]{8}$/;
export const positiveNumberPattern = /^\d+(\.\d+)?$/;
export const positiveIntegerPattern = /^[1-9]\d*$/;

export const twitterPattern = /^\s*@?[a-zA-Z0-9_]{1,25}\s*$/;
