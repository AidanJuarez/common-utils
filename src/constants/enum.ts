export enum TransferDirection {
    IN = 1,
    OUT = 2
}

export enum TransferSource {
    USER = 1,
    TRACK = 2,
    COPY_TRADE = 3,
    GOD_MODE = 4
}

export enum GodModeType {
    Liquidity = 1,
    Method = 2,
    Auto = 3
}

export enum UniswapVerison {
    V2 = 'v2',
    V3 = 'v3'
}
